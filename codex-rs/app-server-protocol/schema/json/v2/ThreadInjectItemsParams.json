{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "items": {
      "description": "Raw Responses API items to append to the thread's model-visible history.",
      "items": true,
      "type": "array"
    },
    "terminalCallId": {
      "description": "Append exactly one matching terminal tool output directly to durable history, even while a turn is active.",
      "type": [
        "string",
        "null"
      ]
    },
    "threadId": {
      "type": "string"
    }
  },
  "required": [
    "items",
    "threadId"
  ],
  "title": "ThreadInjectItemsParams",
  "type": "object"
}